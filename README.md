# Maze.js sample

Install composer and npm dependencies
```bash
composer install
npm install
```

Migrate database
```bash
php artisan migrate
```

Generate `js` and `css` files
```bash
npm run dev
```
